spring:
  application:
    name: notification-service

  mail:
    host: smtp.gmail.com
    port: 587
    username: virupavaraprasad22@gmail.com
    password: vqaw fqfi axog xdee
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

  datasource:
    url: jdbc:mysql://mysql:3306/notification_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
          lower-case-service-id: true

server:
  port: 9095

eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:9090/eureka
    register-with-eureka: true
    fetch-registry: true
  instance:
    prefer-ip-address: true

notification:
  admin:
    email: virupavaraprasad22@gmail.com
